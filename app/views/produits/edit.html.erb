<h1>Modifier produit</h1>

<%= render "form", produit: @produit %>

<br>

<div>

  <%= link_to "Show", @produit, class:"btn btn-secondary" %> 
  <%= link_to "Delete", @produit, data: { turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
  <%= link_to "Back", produits_path, class:"btn btn-dark" %>

</div>
